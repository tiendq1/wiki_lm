# Daily Report - 2026-06-03

## Task: Thiết lập quy trình agent cá nhân

- Nguồn: yêu cầu trực tiếp từ người dùng
- Loại: personal
- Trạng thái: completed
- Đã làm:
  - Tạo `AGENTS.md` để mô tả cách agent đọc nhiệm vụ, cập nhật daily report, ghi lỗi, và lưu command line khi sửa lỗi.
  - Tạo `terminal/fixbug.md` làm nơi lưu command line quan trọng trong quá trình debug/sửa lỗi.
- Kết quả:
  - Workspace đã có quy tắc vận hành ban đầu để người dùng có thể bổ sung thông tin từ từ.
- File/thư mục đã thay đổi:
  - `AGENTS.md`
  - `terminal/fixbug.md`
  - `daily-report/2026-06-03.md`
- Ghi chú cho trí nhớ agent:
  - Người dùng muốn agent đọc nhiệm vụ trong `tasks/`, làm xong thì cập nhật vào `daily-report/`.
  - Nếu có lỗi mobile, agent cần ghi vào `errors/mobile/`.
  - Nếu trong quá trình sửa lỗi có command line quan trọng, agent cần ghi vào `terminal/fixbug.md`.

## Task: Ghi nhận task sửa crash app hdb_host trên simulator

- Nguồn: yêu cầu trực tiếp từ người dùng
- Loại: mobile
- Trạng thái: completed
- Đã làm:
  - Tạo task mobile cho lỗi app `hdb_host` crash khi mở trên simulator.
  - Tạo bản ghi lỗi mobile tương ứng trong `errors/mobile/`.
  - Ghi rõ các phần còn thiếu: stack trace, nguyên nhân chính xác, file đã sửa, command line debug/sửa lỗi.
- Kết quả:
  - WikiLLM đã có bản ghi đầu tiên về một task/lỗi mobile đã xử lý trước đó.
  - Chưa ghi vào `terminal/fixbug.md` vì workspace hiện chưa có command line cụ thể từ lần sửa lỗi `hdb_host`.
- File/thư mục đã thay đổi:
  - `tasks/mobile/2026-06-03-hdb-host-simulator-crash.md`
  - `errors/mobile/2026-06-03-hdb-host-simulator-crash.md`
  - `daily-report/2026-06-03.md`
- Ghi chú cho trí nhớ agent:
  - Người dùng muốn dùng WikiLLM để lưu lại cả task đã xử lý trước đó, không chỉ task mới.
  - Với các task cũ chưa có log kỹ thuật, agent cần ghi nhận phần đã biết và đánh dấu phần cần bổ sung sau, tránh tự suy diễn.
